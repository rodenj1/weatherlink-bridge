apiVersion: v1
kind: Service
metadata:
  name: weatherlink-bridge
  namespace: weather
  labels:
    app: weatherlink-bridge
spec:
  selector:
    app: weatherlink-bridge
  ports:
    - name: metrics
      port: 8080
      targetPort: 8080
      protocol: TCP
